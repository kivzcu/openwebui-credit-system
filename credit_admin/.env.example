# Example environment configuration for Credit Admin
# Copy this file to `.env` and fill in the real values before running
# DO NOT commit your real `.env` with secrets to version control.

# Application
PORT=8081
ENABLE_SSL=true
RELOAD=false

# Security (replace placeholders with strong secrets)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CHANGEME_PASSWORD
SECRET_KEY=CHANGEME_SECRET_KEY
CREDITS_API_KEY=CHANGEME_CREDITS_API_KEY
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database
# Path used by OpenWebUI when using the local sqlite DB
OPENWEBUI_DATABASE_PATH=/root/.open-webui/webui.db

# PostgreSQL Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=CHANGEME_DB_NAME
DB_CREDIT_NAME=CHANGEME_DB_NAME
DB_USER=CHANGEME_DB_USER
DB_PASSWORD=CHANGEME_DB_PASSWORD

# Database URL for OpenWebUI PostgreSQL migration
# Compose DATABASE_URL from the DB_* env values
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Credit System Database URL (can be same as DATABASE_URL)
CREDIT_DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Extensions (for OpenWebUI setup - copy these to your OpenWebUI .env)
# CREDITS_API_PROTOCOL=https
# CREDITS_API_HOST=localhost:8001
# CREDITS_API_SSL_VERIFY=false
# CREDITS_API_KEY=CHANGEME_CREDITS_API_KEY

# Notes:
# - Replace all CHANGEME_* placeholders with secure values before running in
#   development or production.
# - Keep `.env` out of version control. Use `.env.example` to share required
#   variables without secrets.